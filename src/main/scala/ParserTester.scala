import java.io.{File, PrintWriter}

object ParserTester {
  def main(args:Array[String]): Unit = {
//    val testFolder = new File("testFiles")
//    val list = testFolder.listFiles()
//    var results = new Array[(String,String)](0)
//    list.foreach(p => {
//      results = results:+(p.getName, if (Main.test(p)) {
//        "valid"
//      } else {
//        "failed"
//      })
//      println("tested "+p.getName)
//    })
//    val output = new File("output.txt")
//    if(output.exists())
//      output.delete()
//    output.createNewFile()
//    val pw = new PrintWriter(output)
//    results.foreach(p => {
//      pw.write(p._1 + " "+ p._2+"\n")
//    })
//    pw.close()
  }
}
